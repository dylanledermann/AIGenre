# RabbitMQ Cluster IP Service
apiVersion: v1
kind: Service
metadata:
  name: celerybroker
  namespace: celery
  labels:
    app: rabbitmq
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 5672
    targetPort: rabbitmq
  clusterIP: None
  selector:
    app: rabbitmq
--- # RabbitMQ Broker
apiVersion: v1
kind: RabbitmqCluster
metadata:
  name: celerybroker
  namespace: celery
  labels:
    app: rabbitmq
spec:
  replicas: 3
  resources:
    requests:
      cpu: 500m # 1/2 cpu core
      memory: 2Gi # 2 Gebibytes
    limits:
      cpu: 1 # 1 cpu core limit
      memory: 2Gi # 2 Gebibytes